import { makeVar } from "@apollo/client";

export const captionsVar = makeVar(null);
export const captionDetailVar = makeVar(null);
export const captionAdminVar = makeVar(null);
export const allCaptionsVar = makeVar(null);
